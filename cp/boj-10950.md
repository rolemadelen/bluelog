---
from: 'baekjoon'
level: 'easy'
---

```cpp
#include <bits/stdc++.h> 
using namespace std;
int main(void) {
  ios::sync_with_stdio(0);
  cin.tie(0);

  int t, a, b;
  cin >> t;

  for(int i=0; i<t; ++i) {
    cin >> a >> b;
    cout << a + b << endl;
  }

  return 0;
}
```